# ── ASR Engine — Python dependencies ──
setuptools>=69.0.0
wheel
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# ASR — faster-whisper (CTranslate2 backend, 4x faster than transformers on CPU)
faster-whisper==1.0.3
ctranslate2==4.5.0

# Model conversion (transformers needed for converting HF models to CT2 format)
transformers==4.46.3
torch==2.5.1
torchaudio==2.5.1

# Audio handling (librosa for resampling per the technical guide)
soundfile==0.12.1
numpy==1.26.4
librosa==0.10.2.post1

# Translation (NLLB requires sentencepiece + protobuf)
sentencepiece==0.2.0
sacremoses==0.1.1
protobuf>=4.0.0
